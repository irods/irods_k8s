apiVersion: v1
kind: Service
metadata:
  name: {{ include "irods-mysql.fullname" . }}
  labels:
    {{- include "irods-mysql.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: {{ .Values.service.name }}
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: {{ .Values.service.protocol }}
  selector:
    {{- include "irods-mysql.selectorLabels" . | nindent 4 }}
